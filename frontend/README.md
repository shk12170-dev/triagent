# Triagent frontend

React(Vite) 기반 반응형 대시보드. 루트 [README.md](../README.md)의 "프론트엔드 소스를 직접 수정하고 싶다면" 절 참고.

```powershell
npm install
npm run dev     # http://localhost:5173, /api는 8000으로 프록시
npm run build   # ../static 에 빌드 (백엔드가 서빙)
```
